[www]
listen = 9000
; allowed_clients is intentionally unset: php-fpm rejects the literal "any"
; ("Wrong IP address"), and omitting it allows connections from any client.
pm = dynamic
pm.max_children = 16
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 8
clear_env = no
